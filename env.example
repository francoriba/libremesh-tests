# Hardware-in-the-Loop Testing - Environment Configuration
# =========================================================
# Copy this file to .env and customize for your system
# Or source .envrc (direnv) for automatic environment setup

# Hardware Testing Infrastructure Paths
# --------------------------------------

# Path to pi-hil-testing-utils repository (Arduino relay control scripts)
# Default: auto-detected from workspace structure (../pi-hil-testing-utils)
HIL_UTILS_PATH="${HOME}/pi/pi-hil-testing-utils"

# Path to firmware images directory
# Default: auto-detected from workspace structure (../images)
HIL_IMAGES_PATH="${HOME}/pi/images"

# TFTP server root directory (for U-Boot recovery)
# Default: /srv/tftp
HIL_TFTP_ROOT="/srv/tftp"

# Workspace path (usually auto-detected)
# Default: auto-detected from this file's location
HIL_WORKSPACE_PATH="${HOME}/pi/openwrt-23.05.5"

# Device Network Configuration
# --------------------------------------

# Belkin RT3200 #1 (Linksys E8450)
HIL_BELKIN1_IP="192.168.20.182"
HIL_BELKIN1_SERIAL="/dev/belkin-rt3200-1"
HIL_BELKIN1_RELAY_CHANNEL="2"

# Belkin RT3200 #2 (Linksys E8450)
HIL_BELKIN2_IP="192.168.20.183"
HIL_BELKIN2_SERIAL="/dev/belkin-rt3200-2"
HIL_BELKIN2_RELAY_CHANNEL="3"

# GL-iNet MT300N-V2 (Mango)
HIL_GLINET_IP="192.168.20.181"
HIL_GLINET_SERIAL="/dev/glinet-mango"
HIL_GLINET_RELAY_CHANNEL="0"
HIL_GLINET_SERIAL_ISOLATOR_CHANNEL="1"

# Arduino Relay Controller
HIL_ARDUINO_RELAY_DEVICE="/dev/arduino-relay"
HIL_ARDUINO_RELAY_BAUDRATE="115200"

# TFTP Server Configuration (for U-Boot recovery)
HIL_TFTP_SERVER_IP="192.168.20.234"

# Notes:
# ------
# - Paths with ${HOME} will be expanded automatically
# - After editing, run: source tests/.envrc
# - Or use direnv: cd tests && direnv allow
# - Or run setup script: ./tests/setup_environment.sh
# - Device IPs, serial ports, and relay channels are specific to your testbed
# - GL-iNet password is omitted (devices flashed continuously have no password)

